import React from 'react';
import s from './Settings.module.css';

const Settings = (props) => {
    return (
        <div>
            <h2>Settings</h2>
            Security
            Notifications
            Color Themes
            Language
        </div>
    );
};

export default Settings;

